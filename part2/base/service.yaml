apiVersion: v1
kind: Service
metadata:
  name: team-alpha-backend
  namespace: team-alpha
  labels:
    app: team-alpha-backend
spec:
  type: LoadBalancer
  selector:
    app: team-alpha-backend
    slot: green
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
